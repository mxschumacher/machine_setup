automate backup
automate broken symlink repair

generate & store shell history. Only unique commands. Beware of leaked passwords! Really useful for autocompletion in Fish.

create "fresh_install" script: manually get yay, then install everything in the packages list, copy config files
---> test in qemu on a fresh Arch installation
---> setup internet connection, keyboard layout, user accounts, install git. Then pull this script and let it do the rest.

yay -> fish -> omf -> sushi

automate global packages installation from:
- rustup

config + plugins for: vscode, Pycharm, Firefox

there are more todos in scripts/system_maintenance 

get inspiration from the countless system automation and dotfiles repos + blog posts!
